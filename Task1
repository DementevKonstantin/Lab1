#include <stdio.h>

int main()
{ 
  for (int x = 1; x <= 100; x++)
  {
    if ((x % 3 == 0) && (x % 5 != 0))
    {
      printf("Fizz\n");
    }
    if ((x % 3 != 0) && (x % 5 == 0))
    {
      printf("Buzz\n");
    }
    if ((x % 3 == 0) && (x % 5 == 0))
    {
      printf("FizzBuzz\n");
    }
    if ((x % 3 != 0) && (x % 5 != 0))
    {
      printf("%d\n", x);
    }
  }
  
  return 0;
}
